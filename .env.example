# SearXNG Configuration
# =====================

# Secret key for SearXNG (used for sessions, CSRF protection, etc.)
# This should be a long, random string in production
# If not set, SearXNG will generate one automatically
SEARXNG_SECRET=your-super-secret-key-here-change-this-in-production

# Path to SearXNG settings file
# Default: /etc/searxng/settings.yml (set in Dockerfile)
SEARXNG_SETTINGS_PATH=/etc/searxng/settings.yml

# uWSGI Configuration
# ===================

# Number of uWSGI worker processes
# Lower values use less memory, suitable for free tiers
# Default: 1 (set in render.yaml and docker-entrypoint.sh)
UWSGI_WORKERS=1

# Number of threads per uWSGI worker
# Default: 2 (set in render.yaml and docker-entrypoint.sh)
UWSGI_THREADS=2

# Application Port
# ================

# Port for the web server to bind to
# Default: 8080 (set by Render's PORT environment variable)
# If deploying elsewhere, set this to your desired port
PORT=8080

# Additional SearXNG Settings (Optional)
# ======================================

# Redis configuration (if using Redis for caching)
# REDIS_URL=redis://localhost:6379/0

# Database URL (if using a database for preferences/results)
# DATABASE_URL=sqlite:///searxng.db

# Debug mode (set to true for development)
# SEARXNG_DEBUG=false

# Instance name (displayed in the UI)
# SEARXNG_INSTANCE_NAME="My SearXNG Instance"

# Base URL (important for proper link generation behind reverse proxies)
# SEARXNG_BASE_URL=https://your-domain.com
